# Managed by chezmoi — secrets from Dashlane
# DO NOT EDIT DIRECTLY — run `chezmoi edit ~/.secrets.zsh` then `chezmoi apply`
{{ if lookPath "dcli" -}}
export NPM_TOKEN={{ dashlaneNote "npm-token" | trim }}
export NPM_TOKEN_VOLTRAS={{ dashlaneNote "npm-token-voltras" | trim }}
export NPM_TOKEN_TITAN_DESIGN={{ dashlaneNote "npm-token-titan-design" | trim }}
{{ else -}}
# Dashlane CLI (dcli) not found — install with: brew install dashlane-cli
# Then run: dcli sync && chezmoi apply
# For now, set tokens manually below or source from ~/.localrc
{{ end -}}
